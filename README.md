GNURadio flowgraph that decodes SuperDARN pulses running PcodeID somewhat passably. 
Decoding blocks disabled by default, records IQ data instead. RUN SPARINGLY.
LPF block required if using a  rtlsdr v4, other SDRs may need different signal processing to work!

